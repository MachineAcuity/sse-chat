{
	"title": "sse-chat",
	"description": "Realtime Tchat Application using SvelteJS And Server Sent Events",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build-all": "(cd backend/ && cargo build) & (cd frontend && npm run build && cp -R public/ ../backend/public)",
		"dev-backend": "cd backend/ && rm -rf public/* && cargo run",
		"start-all": "cd backend/ && cargo run"
	},
	"workspaces": [ "frontend" ]
}
